apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "statefulset.serviceAccountName" . }}-cleaner
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "statefulset.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
