{{- if eq "prometheus.io/operator" ( include "monitoring.agent" . ) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "taskqueue.fullname" . }}-prometheus-token
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/service-account.name: {{ include "taskqueue.serviceAccountName" . | quote }}
type: kubernetes.io/service-account-token
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "taskqueue.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
  {{- if eq "true" ( include "monitoring.apply-servicemonitor-label" . ) }}
    {{- include "monitoring.servicemonitor-label" . | nindent 4 }}
  {{- else }}
    {{- include "taskqueue.selectorLabels" . | nindent 4 }}
  {{- end }}
spec:
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "taskqueue.selectorLabels" . | nindent 6 }}
  endpoints:
  - bearerTokenSecret:
      name: {{ include "taskqueue.fullname" . }}-prometheus-token
      key: token
    path: /metrics
    port: metrics
    scheme: https
    tlsConfig:
      ca:
        secret:
          name: {{ include "taskqueue.fullname" . }}-cert
          key: ca.crt
      cert:
        secret:
          name: {{ include "taskqueue.fullname" . }}-cert
          key: tls.crt
      insecureSkipVerify: false
      keySecret:
        name: {{ include "taskqueue.fullname" . }}-cert
        key: tls.key
      serverName: "{{ include "taskqueue.fullname" . }}.{{ .Release.Namespace }}.svc"
{{- end }}
