{{- if eq "prometheus.io/operator" ( include "monitoring.agent" . ) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supervisor.fullname" . }}-prometheus-token
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/service-account.name: {{ include "supervisor.serviceAccountName" . | quote }}
type: kubernetes.io/service-account-token
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "supervisor.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
  {{- if eq "true" ( include "monitoring.apply-servicemonitor-label" . ) }}
    {{- include "monitoring.servicemonitor-label" . | nindent 4 }}
  {{- else }}
    {{- include "supervisor.selectorLabels" . | nindent 4 }}
  {{- end }}
spec:
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "supervisor.selectorLabels" . | nindent 6 }}
  endpoints:
  - bearerTokenSecret:
      name: {{ include "supervisor.fullname" . }}-prometheus-token
      key: token
    path: /metrics
    port: metrics
    scheme: https
    tlsConfig:
      ca:
        secret:
          name: {{ include "supervisor.fullname" . }}-cert
          key: ca.crt
      cert:
        secret:
          name: {{ include "supervisor.fullname" . }}-cert
          key: tls.crt
      insecureSkipVerify: false
      keySecret:
        name: {{ include "supervisor.fullname" . }}-cert
        key: tls.key
      serverName: "{{ include "supervisor.fullname" . }}.{{ .Release.Namespace }}.svc"
{{- end }}
